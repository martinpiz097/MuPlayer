package cl.estencia.labs.muplayer.core.bus.message;

public enum MuPlayerTopic {
    PLAYER_RESPONSE,
    START,
    RELOAD,
    PLAY_NEXT,
    PLAY_PREVIOUS,
    PLAY_INDEX,
    PLAY_FOLDER,
    PLAY,
    SHUTDOWN,
    PAUSE,
    RESUME,
    STOP,
    SEEK_SECONDS,
    SKIP_TRACKS,
    SEEK_FOLDER,
    GOTO,
    MUTE,
    UNMUTE,
    GET_VOLUME,
    SET_VOLUME
}
