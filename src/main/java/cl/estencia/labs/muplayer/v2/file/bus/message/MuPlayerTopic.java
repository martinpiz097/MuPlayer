package cl.estencia.labs.muplayer.v2.file.bus.message;

public enum MuPlayerTopic {
    START,
    PLAY_NEXT,
    PLAY_PREVIOUS,
    PLAY_INDEX,
    PLAY,
    SHUTDOWN,
    PAUSE,
    RESUME,
    STOP,
    SKIP_TRACKS,
    MUTE,
    UNMUTE,
}
